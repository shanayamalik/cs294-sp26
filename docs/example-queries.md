# Supported Query Examples (MVP)

1. `section:ABSTRACT AND contains:"wireless device"`
2. `section:SPECIFICATION AND contains:"signal processing"`
3. `section:CLAIMS AND contains:"processor"`
4. `section:SPECIFICATION AND contains:"calibration"`
5. `section:SPECIFICATION AND contains:"embodiment"`
6. `section:OTHER AND contains:"priority"`
7. `section:SPECIFICATION AND contains:"training data"`
8. `section:CLAIMS AND contains:"non-transitory"`
